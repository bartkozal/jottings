<p>
  We appreciate any feedback. You can vote below which features you miss
  the most:
</p>

<%= form_for Feedback.new, url: feedback_path, html: { class: "form-stacked form-poll" } do |form| %>
  <%= label_tag nil, class: "margin-1/3-bottom" do %>
    <%= hidden_field_tag "feedback[answers][apps]", 0 %>
    <%= check_box_tag "feedback[answers][apps]" %> Mobile and Desktop apps
  <% end %>

  <%= label_tag nil, class: "margin-1/3-bottom" do %>
    <%= hidden_field_tag "feedback[answers][sync]", 0 %>
    <%= check_box_tag "feedback[answers][sync]" %> Syncing with external services like Google Drive, Dropbox etc.
  <% end %>

  <%= label_tag nil do %>
    <%= hidden_field_tag "feedback[answers][editor]", 0 %>
    <%= check_box_tag "feedback[answers][editor]" %> More editing options like autoformatting todos, attaching files etc.
  <% end %>

  <%= form.label :comment, "Anything else would you like to add?" %>
  <%= form.text_area :comment, class: "1/1", rows: 4 %>

  <div class="grid grid-center">
    <div class="media media-middle">
      <%= form.submit "Send feedback" %>
      <div class="media-body text-muted text-small">
        Thank you!
      </div>
    </div>
  </div>
<% end %>
