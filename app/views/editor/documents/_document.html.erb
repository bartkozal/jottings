<div class="grid">
  <div class="grid-item 3/4">
    <%= form_for [:editor, document], method: form_method, html: { class: 'form-stacked' } do |form| %>
      <%= form.text_area :body, class: "1/1 margin-bottom", is: "editor", ref: "editor" %>

      <div>
        <%= form.submit "Save" %>
      </div>
    <% end %>
  </div>

  <div class="grid-item 1/4">
    <!-- track edits -->
  </div>
</div>
